/**
 * One of the elements is missing in the arithmetic progression
 *
 * and you gotta find that number
 *
 * find([3, 9, 1, 11, 13, 5]) # => 7 -> 7 is missing in this pattern
 */

function find(seq) {
  const sortedSeq = seq.sort((a, b) => a - b);



  return null;
}

console.log(find([2, -2, 8, -8, 4, -4, 6, -6]));
console.log(find([3, 9, 1, 11, 13, 5]));